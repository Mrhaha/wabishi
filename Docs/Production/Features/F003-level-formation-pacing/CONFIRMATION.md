# F003 确认记录

状态：已确认
功能：F003 关卡与成型节奏优化
最后更新：2026-06-11

## 用户确认

2026-06-11：用户确认采用 `EXECUTION_REVIEW.md` 中的默认补丁组。

## 已确认内容

- `stage_clear_base_gold = 2`。
- `roll_left_gold_bonus = 0`、`cheat_left_gold_bonus = 0` 首版保持关闭。
- 初始金币首版不改，仍以当前配置为准。
- 第 1-2 章普通市场刷新费采用 `1`。
- 第 1-2 章 Boss 后章节市场刷新费建议采用 `2`。
- 不做“每个市场第一次刷新免费”。
- 渐进开放首版使用 `DiceKingDemo.cs` 代码常量 / helper，不新增解锁 CSV。
- 第 1 章类型池保留 `Basic / Odd / Even / Piggy / Turtle / Double`。
- `Double` 作为低权重直观强力件保留，但验证报告需要重点检查价格。
- 隐藏倾向槽位采用 `70%` 当前路线候选池、`30%` 通用已解锁池。
- 路线优先级采用：本市场刚购买的非基础骰 > 当前骰袋主路线 > 通用池。
- 隐藏倾向不新增存档字段。
- F003-04 必须覆盖目标曲线、章节开放、隐藏倾向、经济流水和 UI 信息量验证。
