# F003 验证报告

状态：F003-01 / F003-02 / F003-03 静态验证通过，完整运行验证待执行
功能：F003 关卡与成型节奏优化
来源：F003-04 验证报告切片

## 报告目的

本文件记录 F003 当前实现后的验证结果。当前覆盖 F003-01 数据调参、渐进开放过滤、F003-02 隐藏倾向槽位 / 路线识别和 F003-03 经济配置的静态验证；市场 UI 截图和 Unity Play Mode 采样尚未执行，不能据此声明 F003 整体验收通过。

## 执行前提

- F003-01 数据调参已完成静态实现。
- F003-03 经济配置已完成静态实现。
- 章节开放过滤已接入 `DiceKingDemo.cs` 的市场生成路径。
- F003-02 隐藏倾向槽位和路线识别已接入 `DiceKingDemo.cs` 的市场生成路径。
- 当前验证基于实际 CSV 和代码静态检查，不把生产包提案当作运行事实。

## 目标曲线检查

| 指标 | 结果 | 备注 |
|---|---|---|
| 全 40 关目标表已输出 | 通过 | `chapter_score_table.csv` 共 10 章，每章 3 个普通关 + 1 个 Boss 关 |
| 章内增长率已检查 | 通过 | 每章目标按首关、第二关、第三关、Boss 逐步上升 |
| Boss/首关比已检查 | 通过 | 每章 Boss / 首关约为 `1.60` |
| 下一章首关/上一章 Boss 比已检查 | 通过 | 第 2-10 章首关约为上一章 Boss 的 `0.90-0.91`，保留章初喘息 |
| 异常点解释 | 无异常 | 当前曲线符合 F003 的“章初喘息、章内加压、Boss 检验”结构 |

| 章节 | 关 1 | 关 2 | 关 3 | Boss | Boss/首关 | 下一章首关/本章 Boss |
|---|---:|---:|---:|---:|---:|---:|
| 第 1 章 | 72 | 84 | 99 | 115 | 1.60 | 0.90 |
| 第 2 章 | 104 | 122 | 142 | 166 | 1.60 | 0.91 |
| 第 3 章 | 151 | 177 | 207 | 242 | 1.60 | 0.90 |
| 第 4 章 | 219 | 256 | 300 | 350 | 1.60 | 0.91 |
| 第 5 章 | 318 | 372 | 436 | 509 | 1.60 | 0.91 |
| 第 6 章 | 462 | 541 | 633 | 739 | 1.60 | 0.91 |
| 第 7 章 | 671 | 785 | 919 | 1074 | 1.60 | 0.91 |
| 第 8 章 | 974 | 1140 | 1334 | 1558 | 1.60 | 0.91 |
| 第 9 章 | 1414 | 1654 | 1937 | 2262 | 1.60 | 0.91 |
| 第 10 章 | 2053 | 2402 | 2813 | 3285 | 1.60 | 不适用 |

## 章节开放检查

采样要求：第 1、2、3、4、7、10 章各至少 100 次市场刷新。

当前结论：已完成代码静态检查，尚未完成 Unity 刷新采样。`PickMarketDieConfig`、`PickDiceMaterial` 和 `PickFaceTemplate` 均在抽取前应用章节开放过滤，未解锁内容静态路径不会进入候选池。

| 章节 | 类型池结果 | 材质池结果 | 面组模板结果 | 未解锁出现次数 | 结论 |
|---|---|---|---|---|---|
| 第 1 章 | `Basic / Odd / Even / Piggy / Turtle / Double` | `OfficialIron / GiltSeal` | `balanced / low_dense / high_bias` | 静态为 0 | 静态通过，待采样 |
| 第 2 章 | 第 1 章 + 路线补强、`Tree` | 第 1 章 + `ClearGlaze / CopperBone` | 第 1 章 + `same_focus / straight_patch` | 静态为 0 | 静态通过，待采样 |
| 第 3 章 | 开放当前全部已配置类型 | 开放当前全部已配置材质 | 开放当前全部已配置模板 | 静态为 0 | 静态通过，待采样 |
| 第 4 章 | 全池 | 全池 | 全池 | 静态为 0 | 静态通过，待采样 |
| 第 7 章 | 全池 | 全池 | 全池 | 静态为 0 | 静态通过，待采样 |
| 第 10 章 | 全池 | 全池 | 全池 | 静态为 0 | 静态通过，待采样 |

## 隐藏倾向槽位检查

采样要求：奇数、偶数、金币、乌龟、大树、爆发各至少 100 次。

当前结论：已完成代码静态检查，尚未完成 Unity 刷新采样。代码路径显示：每次整批生成 3 个货架时随机 1 个后台隐藏倾向槽位；倾向槽位在有路线可推断时以 `70%` 尝试路线候选池、`30%` 使用通用已解锁池；候选为空、未解锁或与同屏货架冲突时回退通用池。路线来源优先级为本市场刚购买的非基础骰路线、当前骰袋唯一数量最多的非基础路线、通用池。

静态路线池检查：

| 路线 | 第 1 章可倾向候选 | 第 2 章可倾向候选 | 第 3 章后补齐 |
|---|---|---|---|
| 奇数 | `Odd` | `Odd / LoneWitness / Stamp` | 无新增 |
| 偶数 | `Even` | `Even / HalfStep / Track` | 无新增 |
| 金币 | `Piggy` | `Piggy / Treasury / Bribe` | `Investment` |
| 乌龟 | `Turtle` | `Turtle / Shellsmith / SlowTurtle` | `Nest` |
| 大树 | 无，回退通用池 | `Tree` | `Gardener / Irrigation` |
| 爆发 | `Double` | `Double` | `Gambler` |

| 路线 | 样本数 | 相关候选出现率 | 通用池对照 | 三槽同路线风险 | 结论 |
|---|---:|---:|---:|---|---|
| 奇数 | 待运行采样 | 静态路径可走路线池 | 通用池可回退 | 静态风险低 | 待 Unity 采样 |
| 偶数 | 待运行采样 | 静态路径可走路线池 | 通用池可回退 | 静态风险低 | 待 Unity 采样 |
| 金币 | 待运行采样 | 静态路径可走路线池 | 通用池可回退 | 静态风险低 | 待 Unity 采样 |
| 乌龟 | 待运行采样 | 静态路径可走路线池 | 通用池可回退 | 静态风险低 | 待 Unity 采样 |
| 大树 | 待运行采样 | 静态路径可走路线池 | 通用池可回退 | 静态风险低 | 待 Unity 采样 |
| 爆发 | 待运行采样 | 静态路径可走路线池 | 通用池可回退 | 静态风险低 | 待 Unity 采样 |

## 经济流水检查

已确认并实现的默认值：

- `stage_clear_base_gold = 2`
- `roll_left_gold_bonus = 0`
- `cheat_left_gold_bonus = 0`
- 第 1-2 章普通市场 `normal_refresh_cost = 1`
- 第 1-2 章 Boss 后章节市场 `boss_refresh_cost = 2`

静态价格区间按当前类型基础价 + 面组修正 + 材质修正计算。实际收入仍需要 Play Mode 检查通关结算预览和到账数值是否一致。

| 场景 | 预览收入 | 实际收入 | 刷新成本 | 购买价格区间 | 结论 |
|---|---:|---:|---:|---|---|
| 第 1 章普通关后市场 | `+2` 固定金币 | 待运行验证 | `1` | `3-12` | 配置通过，待运行验证 |
| 第 1 章 Boss 后市场 | `+2` 固定金币 | 待运行验证 | `2` | `3-12` | 配置通过，待运行验证 |
| 第 2 章普通关后市场 | `+2` 固定金币 | 待运行验证 | `1` | `3-15` | 配置通过，待运行验证 |
| 第 2 章 Boss 后市场 | `+2` 固定金币 | 待运行验证 | `2` | `3-15` | 配置通过，待运行验证 |

## 购买可负担性检查

| 检查项 | 结果 | 结论 |
|---|---|---|
| 第 1 章入口路线骰成交价区间 | `Basic 3-7`，`Odd / Even 5-9`，`Piggy 6-10`，`Turtle 8-12` | 明显低于原先早期叠价压力 |
| 第 1 章 `Double` 成交价区间 | `8-12` | 已保留但不再高到不可触达 |
| 第 1 章刷新 1 次后是否仍有购买空间 | 静态估算：首关后现金流 +2，普通刷新费 1，仍可购买基础入口件 | 需要真实游玩采样验证 |
| 第 2 章补强骰成交价区间 | `8-15` | 能作为第二章补强池出现，仍需采样看体感密度 |

## UI 信息量检查

当前结论：未执行截图检查。静态实现减少了第 1 章可出现的类型、材质和面组模板，并且隐藏倾向没有新增市场 UI 文案；但尚未用 Unity 截图验证商品卡文本、材质说明和市场布局的信息量。

| 检查项 | 结果 | 结论 |
|---|---|---|
| 第 1 章市场截图 | 待执行 | Play Mode 后补 |
| 不显示“推荐路线”“倾向槽位”等明示标识 | 静态通过 | 代码未新增明示倾向 UI 文案，待截图复核 |
| 商品卡说明不明显溢出 | 待执行 | 需要截图 |
| 类型、材质、面组信息量可读 | 待执行 | 需要截图 |

## Unity 验证

当前命令行环境未找到可用的 Unity 批处理入口、`.sln` 或 `dotnet` 编译入口，因此本轮只完成静态验证。

| 验证项 | 结果 | 备注 |
|---|---|---|
| Play Mode 可进入市场 | 待执行 | 需要 Unity |
| 刷新、购买、卖出正常 | 待执行 | 需要 Unity |
| 通关收入预览和实际收入一致 | 待执行 | 需要 Unity |
| 继续游戏不依赖隐藏倾向持久化 | 静态通过 | F003-02 未新增存档字段，待 Play Mode 复核 |

## 当前结论

F003-01 数据调参、F003-02 市场生成规则和 F003-03 经济配置已完成静态实现并通过 CSV / 代码路径检查。当前尚不能声明 F003 整体验收通过；下一步应用 Unity Play Mode 补齐市场采样、经济流水和 UI 截图验证。
