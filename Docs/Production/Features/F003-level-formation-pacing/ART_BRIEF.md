# F003 美术需求

状态：已确认，待实现
功能：F003 关卡与成型节奏优化
关联美术文档：PROJECT_CONTEXT.md、ART_ASSETS.md

## 输入决策

- F003 首版不新增骰子类型、材质或独立美术资源。
- 市场软引导必须隐藏，不应生成明示路线锁定的图标、徽章或专属货架美术。
- 内容渐进开放可以依靠现有类型图标、材质 shader、文本和数据过滤实现。

## 视觉意图

首版美术目标是“不增加视觉负担”。F003 解决的是关卡节奏、市场成型和信息量问题，不需要通过新增资源来表达。现有骰子类型图标和 F002 材质 shader 继续作为市场商品视觉基础。

## 资源清单

| 资源 | 用途 | 尺寸 / 格式 | 目标路径 | 来源决策 | 状态 |
|---|---|---|---|---|---|
| 无新增资源 | 首版不需要新增图标或面板 | 不适用 | 不适用 | F003 首版范围 | 已后置 |

## 风格关键词

- 沿用 `Bright Ledger Boardgame` / `明亮账本桌游风`。
- 保持轻量、清楚、可读。
- 市场商品仍以骰子类型图标、面组、材质表现和短规则为主。

## 必须保留

- 现有 `Assets/Resources/Art/DiceTypes/` 骰子类型图标。
- 现有 `Assets/Resources/Shaders/DiceMaterialOverlay.shader` 运行时材质表现。
- 现有市场 UI 的账本桌面风格。

## 避免

- 不要为隐藏倾向槽位生成“推荐”“路线”“命运安排”等显式图标。
- 不要新增路线阵营大标识，避免玩家误以为已经选择或锁定路线。
- 不要为渐进开放额外加入教程式视觉框。

## 生成 / 来源备注

首版无需调用 `$wabish-art-production` 或 `$wabish-art-assets`。若后续审查要求新增“章节开放提示”或“市场阶段变化”资源，应另起美术切片，不阻塞 F003 首版程序实现。

## 放置 / 运行时用途

无新增放置要求。程序只需要按章节过滤已存在的类型、材质和面组模板。

## 实现备注

如果界面体验需要提示内容渐进开放，优先使用已有文本区域和现有小图标，不生成新图。

## 阻塞项

无。

## 验收

- F003 首版未新增美术资源。
- 市场界面中不存在明示隐藏倾向槽位的视觉标记。
- 现有骰子类型图标和材质表现仍可正常显示。
