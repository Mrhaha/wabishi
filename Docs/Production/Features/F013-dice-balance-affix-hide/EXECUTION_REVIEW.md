# F013 执行审查

状态：F013-03 首轮候选已静态接入，整体运行待验证
生产包：Docs/Production/Features/F013-dice-balance-affix-hide/
来源设计对话编号：F013
最后更新：2026-06-16

## 审查结论

就绪度：软关闭静态接入，运行待验证

原因：用户已接受 F013 的软关闭默认值，生产包已明确“不显示也不生效、旧存档保留但暂不生效”的行为边界；美术无需新增资源，界面状态和程序入口均已列出，验收标准可观察。当前没有阻塞程序执行的问题。

## 摘要

F013 可以进入 `$wabish-dev-implementation`。执行时必须先做词缀与改造道具软关闭，再做无词缀基线重平衡，不能直接调关卡或市场数值。当前审查只更新生产包文档，不改代码、不改 CSV、不更新主文档当前事实。

## 建议改进

### 推荐生产包补丁

已应用以下补丁：

- 将软关闭默认值从待确认提升为已确认。
- 增加 `CONFIRMATION.md`，记录用户在 2026-06-16 接受软关闭默认值。
- 将 `FEATURE_BRIEF.md`、`DEV_HANDOFF.md`、`EXECUTION_PLAN.md`、`ACCEPTANCE.md` 中的软关闭阻塞项移除。
- 将美术、界面体验、程序就绪度更新为可执行状态。
- 保留正式起手金币、目标曲线和市场权重候选值为执行阶段产物，不作为审查阻塞。

### 需要用户决策的阻塞问题

无。

### 非阻塞后置细节

- 正式起手金币候选值仍需 F013 执行阶段提出并由用户验收。
- 目标曲线、市场价格、刷新费用、材质权重和骰子权重需要在无词缀基线下完成候选调参。
- `market_test_random_refresh = 0` 建议作为正式市场采样基线；如果执行阶段使用测试随机，需要在验收记录中分开标注。

## 职能就绪度

| 角色 | 状态 | 原因 | 下一步 skill |
|---|---|---|---|
| 策划 | 就绪 | 核心范围、软关闭行为和非目标已确认；数值候选可在执行阶段产出 | `$wabish-dev-implementation` |
| 美术 | 就绪 | 不需要新资源，F005 道具图标只保留不展示 | 无需美术执行 |
| 界面体验 | 就绪 | 市场、骰袋、结果决策和结算展示的隐藏状态清楚 | `$wabish-dev-implementation` |
| 程序 | 就绪 | 代码入口、数据影响、存档边界、验收检查均已列出 | `$wabish-dev-implementation` |

## 执行顺序

1. F013-01：清点当前数值、市场、UI、结算和存档入口。
2. F013-02 / F013-04：实现软关闭和界面隐藏，确保词缀不再作为可见或隐形变量。
3. F013-03：在无词缀状态下提出首轮经济、关卡、市场和材质候选值。
4. F013-05：做静态校验、Unity Play Mode、截图验收和主文档同步。

## 建议写回生产包的补丁

| 文件 | 章节 | 建议改动 | 原因 | 状态 |
|---|---|---|---|---|
| FEATURE_BRIEF.md | 决策矩阵 / 已确认规则 / 待回答问题 | D4 / D5 提升为已确认，移除软关闭阻塞 | 用户已接受默认值 | 已应用 |
| DEV_HANDOFF.md | 输入决策 / 阻塞项 | 写入软关闭已确认，移除条件式阻塞 | 程序执行不应再次猜测核心行为 | 已应用 |
| EXECUTION_PLAN.md | 就绪度 / 执行顺序 / 阻塞问题 | 改为程序就绪，删除审查前置步骤 | 本轮即为审查结果 | 已应用 |
| ACCEPTANCE.md | 就绪度门禁 / 已知缺口 / 最终结论 | 移除软关闭未确认缺口 | 验收缺口只保留未实现和未验证项 | 已应用 |
| ART_BRIEF.md | 状态 | 改为美术就绪 | 无新资源需求 | 已应用 |
| UI_UX_SPEC.md | 状态 / 阻塞项 | 改为界面就绪，移除已解决条件 | 软关闭默认已确认 | 已应用 |
| CONFIRMATION.md | 全文 | 新增确认记录 | 支持状态门禁和后续追溯 | 已应用 |

## 风险

| 风险 | 影响 | 缓解方式 | 负责人 |
|---|---|---|---|
| 只关闭市场道具但忘记关闭结算词缀 | 隐藏变量继续污染平衡样本 | `AffixScoreBonusForDie` 和 `WalletIncomeForDie` 必须受开关保护 | 程序 |
| 只依赖 `market_test_random_refresh = 1` | 正式市场关闭后改造道具可能重新出现 | 新增独立词缀开关，不把测试随机当隐藏方案 | 程序 |
| UI 隐藏后留下空按钮或错位 | 市场截图不可验收 | `1280x720` 和 `1920x1080` 截图验收 | 界面体验 / 程序 |
| 1000 金测试起手继续参与调参 | 正式经济判断失真 | F013-03 必须提出正式起手金币候选并单独验收 | 策划 / 程序 |
| 主文档提前同步未来行为 | 当前事实与文档冲突 | 等代码和 CSV 实现后再同步主文档 | 程序 |

## 已记录的用户决策

- 2026-06-16：用户接受 F013 的软关闭默认值。
- 2026-06-16：隐藏期间词缀不显示也不生效。
- 2026-06-16：旧存档词缀和三类改造道具数量保留，但隐藏期间不展示、不允许使用、不参与结算。
- 2026-06-16：本轮只做包审查，不改代码、不改 CSV。

## 最终建议

F013 可进入程序执行。建议下一句调用：

```text
使用 $wabish-dev-implementation 执行 F013-dice-balance-affix-hide 的 F013-01、F013-02 和 F013-04。先实现词缀与改造道具软关闭和 UI 隐藏，不做数值重平衡。
```

## 执行更新

- 2026-06-16：已执行 F013-01 / F013-02 / F013-04。
- 2026-06-16：已新增 `affix_feature_enabled = 0`，并将市场生成/购买/使用、市场 UI、骰袋显示、结果短标签、前缀加分和后缀金币收益接入软关闭。
- 2026-06-16：已保留 F005 CSV、图标、旧存档词缀字段和三类改造道具数量。
- 2026-06-16：已接入 F013-03 无词缀数值基线首轮候选：`starting_gold = 8`、`stage_clear_base_gold = 3`、`market_test_random_refresh = 0`。
- 2026-06-16：按用户要求修正前三章过平和第三章后断崖问题，前三章目标改为第一章 `60 / 74 / 70 / 70`、第二章 `78 / 70 / 86 / 82`、第三章 `100 / 88 / 82 / 94`；第四章改为 `104 / 115 / 127 / 140`，终章改为 `1141 / 1261 / 1393 / 1539`。
- 2026-06-16：按用户后续反馈静态接入市场刷新二次调整：`Basic` 退出随机货架，F008/F010/F011 开放后权重提高，付费刷新优先避开上一批货架类型。
- 2026-06-16：CSV 解析通过；Unity Play Mode、截图验收、旧存档构造验证、正式市场连续刷新样本和前 3 章实机样本未执行。
