# F006 结算演出与槽位顺序

状态：已确认
负责人角色：策划
范围：策划 / 美术 / 界面体验 / 程序
来源：Docs/DesignDialogues/F006-settlement-performance-sequence.md
设计对话编号：F006
关联主文档：PROJECT_CONTEXT.md、GAME_FLOW.md、DICE_ARCHETYPES.md
关联代码：Assets/Scripts/DiceKingDemo.cs
关联数据：无
关联资源：现有 `Assets/Resources/Art/` 运行时资源；首版不强制新增资源
Notion 生产包：https://app.notion.com/p/37df9be4dcfd81aa824ae9b3898f88b2
最后更新：2026-06-12

## 功能意图

F006 解决结算演出仍然偏素、分数来源归因不够清楚的问题。当前 F007 已经接入左端双数字计分器和左到右跳涨，但玩家看到的主要还是数字变化，缺少开场定格、当前槽位焦点、路线触发、倍率重击和通关收束这些情绪层级。

本包目标是把结算从“逐颗跳字”升级为“五拍结算舞台”：结果定格、逐槽点名、路线小爆点、倍率盖章、目标冲刺与收束。该升级只改变展示和节奏，不改变真实计分公式、骰面概率、出千规则、目标曲线、经济、存档或骰子效果。

## 玩家体验

玩家确认结算后，骰子不再重新排序，仍按投骰前的固定槽位从左到右过账。每一颗骰子被点名时，投掷区只突出当前槽位、当前分数变化和最重要的触发标签。普通骰快速入账，路线关键效果慢半拍，倍率成立时以更重的倍率章反馈把分数推高，最后目标数字滚动到真实结果。

玩家应该能在结算结束后说出本次高分主要来自哪几个节点，例如“第三颗盖章触发”“第五颗形成顺子倍率”“龟龟补了几颗小骰”“最后贿赂补到过关”。演出要让构筑贡献被看见，而不是让所有触发都挤成日志。

## 决策矩阵

| 编号 | 决策 / 问题 | 状态 | 负责人 | 来源 | 下游影响 | 动作 |
|---|---|---|---|---|---|---|
| D1 | 结算顺序使用当前骰袋槽位顺序，不使用按点数排序后的展示顺序。 | 已确认 | 策划 / 程序 | F006 已确认决策、GAME_FLOW.md | 真实结算、动画队列、流式钱包 | 执行 |
| D2 | 结果锁定、孤证重摇和出千重摇都不改变槽位顺序。 | 已确认 | 程序 | F006 已确认决策、GAME_FLOW.md | 投掷区展示、出千后结算 | 执行 |
| D3 | 猪猪、鎏印、金币后缀、贿赂和后续读取钱包对象按槽位从左到右结算。 | 已确认 | 策划 / 程序 | F006 已确认决策、PROJECT_CONTEXT.md | 金币流、贿赂可用钱包、验收样例 | 执行 |
| D4 | F006 不改真实计分、骰面随机、出千、目标曲线、数据表或存档版本。 | 已确认 | 跨职能 | F006 设计理由、F007 实现事实 | 程序边界、验收范围 | 执行 |
| D5 | 结算演出采用“五拍结算舞台”作为首版方向。 | 已确认 | 策划 / 界面体验 | 用户认可方案、F006 第 6 轮 | UI_UX_SPEC.md、DEV_HANDOFF.md | 执行 |
| D6 | 后台先冻结真实结算提交态，动画只消费提交态或展示事件，不在播放中重新计算会影响结果的逻辑。 | 已确认 | 程序 | F006 职能备注、GAME_FLOW.md | 展示事件列表、跳过和加速安全性 | 执行 |
| D7 | 普通骰快速入账，路线关键触发慢半拍，倍率和通关收束是主要爆点。 | 已确认 | 界面体验 / 程序 | F006 倾向方向、用户认可方案 | 演出节奏、验收感受 | 执行 |
| D8 | 首版路线小爆点优先覆盖金币入账、龟龟临时小骰、大树成长、倍率成立。 | 已确认 | 界面体验 / 程序 | F006 第 5 轮、用户认可方案 | 事件类型、视觉反馈优先级 | 执行 |
| U1 | 普通一手总时长建议控制在约 1.8 到 2.4 秒，爆发手可放宽到约 3 秒。 | 已确认 | 界面体验 / 程序 | F006 审查补丁 | 动画配置、Play Mode 手感 | 首轮采用默认值并验收 |
| U2 | 是否加入 `Space` 加速结算。 | 已后置 | 界面体验 / 程序 | F006 审查补丁 | 输入流程、动画状态机 | 不进首轮，保留 F006-04 |
| U3 | 是否加入音效和桌面震动。 | 已后置 | 美术 / 程序 | F006 审查补丁、F004 后置项 | 资源与运行时反馈 | 首版后置 |
| U4 | 点数摘要条的具体位置。 | 已后置 | 界面体验 | F006 审查补丁 | 牌型阅读辅助 | 首版沿用现有牌型提示 |
| X1 | 按点数排序后的展示顺序不作为真实结算队列。 | 已后置 | 策划 / 程序 | F006 已拒绝方案 | 防止规则回退 | 后置 |
| X2 | 全量专属路线特效、最终音效包、震动和完整跳过系统不进入首版必做范围。 | 已后置 | 跨职能 | F006 范围收敛 | 生产范围、实现风险 | 后置 |

## 已确认规则

- 投骰前的 `Ready` 阶段允许玩家调整骰子槽位。
- 开盅、孤证重摇、出千重摇和结算开始后，不按点数重排骰子。
- 左到右结算指当前骰袋槽位顺序。
- 结算演出必须读取真实结算后的提交态或由提交态派生的展示事件，不得在动画播放中改变随机、计分或钱包结果。
- 五拍结算舞台的顺序为：结果定格、逐槽点名、路线小爆点、倍率盖章、目标冲刺与收束。
- 当前槽位是结算期唯一主焦点，同一时刻不要让所有骰子、日志和目标数字一起闪。
- 每个槽位旁只显示最重要的短反馈，例如 `+6`、`盖章 6`、`金币 +1`、`成长待入档`。
- 通关收束和未达标收束必须有视觉差异；通关可以盖章，未达标保持安静。
- 大树成长应在分数落定后表现为“成长入档”，避免玩家误以为成长已经影响本次基础点数。

## 建议默认值

- 首版使用现有 OnGUI、现有骰子图标、现有投掷区和左端计分器，不新增运行时贴图。
- 普通槽位入账节奏约 0.18 到 0.25 秒，路线高光额外停顿约 0.15 到 0.25 秒，最终收束约 0.35 秒。
- 普通手目标约 2 秒，爆发手最多约 3 秒，具体手感以后续 Play Mode 验收为准。
- `Space` 加速不进入首轮；若后续执行 F006-04，按住或再次按下后可以缩短剩余演出，但必须直接落到同一最终提交态。
- 音效和桌面震动后置；程序可先预留事件类型，不实际接音频资源。
- 点数摘要首版沿用现有牌型提示和牌型表入口，不新增常驻摘要条。

## 待回答问题

无阻塞待回答问题。

以下后置项不影响 F006-02 / F006-03 首轮执行：

- `Space` 加速结算是否在 F006-04 单独执行。
- 音效和桌面震动是否在后续表现包执行。
- 是否在后续版本新增常驻点数摘要条。

## 阻塞项

当前没有阻塞程序实现的问题。节奏、加速和音效都已有建议默认值或后置边界，审查补丁已完成，可交给 `$wabish-dev-implementation` 执行。

## 约束

- 不改变 `ScoreDice()` 的真实结果口径。
- 不改变 `RollPhase` 的核心流程语义，除非程序为展示增加内部子阶段。
- 不改变 `SaveVersion`、`PlayerPrefs` 存档字段或市场数据。
- 不新增需要玩家理解的新规则。
- 不把日志搬到主投掷区形成说明面板。
- 不用大段文字解释每个触发，主反馈必须短。
- 不把通关和失败演出做成同一种强烈反馈。

## 非目标 / 后置范围

- 完整重做主流程 UI。
- 新增结算奖励、连击、额外金币或额外追分规则。
- 新增全局牌型或改变倍率表。
- 为每个骰子类型制作专属动画资产。
- 最终音效、桌面震动、手柄震动。
- 完整结算回放系统。
- 复杂跳过设置页。

## 执行切片

| 切片 | 范围 | 输出 | 当前状态 |
|---|---|---|---|
| F006-01 | 生产包审查与范围锁定 | 审查后确认五拍范围、建议默认值和后置项 | 已完成 |
| F006-02 | 结算展示事件列表 | 从 `ScoreDice()` 后提交态派生展示事件，事件包含槽位、触发类型、分数、金币、倍率、成长和高光等级 | 待执行 |
| F006-03 | 五拍视觉节奏接入 | 结果定格、逐槽点名、路线高光、倍率盖章、目标收束在 OnGUI 中可见 | 待执行 |
| F006-04 | 加速结算增强 | 可选 `Space` 加速或跳过剩余演出，直接应用最终提交态 | 已后置 |
| F006-05 | Unity Play Mode 与截图验收 | 验证普通手、倍率手、金币、龟龟、大树、贿赂、通关和未达标 | 待执行 |

## 职能交接

### 美术

首版不强制新增运行时资源。美术只需要约束视觉语言：明亮账本桌游风、账本盖章、金币飞行、倍率章压印、成长入档。若后续需要新贴图，应先由 `$wabish-art-production` 生成资源清单和接触图，再写入 `ART_ASSETS.md`。

### 界面体验

界面体验负责把五拍拆成可读状态。当前槽位、基础分、倍率和目标数字要按同一节拍脉冲。路线触发只能短提示，不要把日志和说明面板塞回主流程。

### 程序

程序应在 `Assets/Scripts/DiceKingDemo.cs` 中沿用当前 `scoringDice`、`RunScoreCounterStep`、`BeginSettle()` 和 `UpdateScoreReveal()` 的提交态基础，新增展示事件或扩展现有展示快照。动画播放、加速和最终落点必须消费同一份提交态。

## 不要改动

- 不恢复按点数排序结算。
- 不改变投骰概率和骰面随机。
- 不改变真实分数、金币、成长或贿赂结果。
- 不改变 F007 左端积分塔的主结构。
- 不新增常驻第三个主数字。
- 不把临时小骰折叠规则改成全量拖长播放。

## 同步目标

- `Docs/DesignDialogues/F006-settlement-performance-sequence.md`：补充生产包路径和当前状态。
- `PROJECT_CONTEXT.md`：实现完成后同步 F006 演出事实。
- `GAME_FLOW.md`：实现完成后同步结算动画和加速规则事实。
- `DICE_ARCHETYPES.md`：仅当实现改变骰子结算边界时才更新；本包首版不应触发。
- Notion：创建或更新 `Wabish 生产包 F006 - 结算演出与槽位顺序`。

## 验收含义

F006 验收通过意味着：结算从当前单纯逐颗跳字升级为有明确节奏的五拍舞台；玩家能看到当前槽位、路线触发、倍率成立和目标达成的先后关系；最终显示和真实结算结果一致；普通结算不拖慢，爆发结算有清晰爽点；主流程没有新增规则负担。
