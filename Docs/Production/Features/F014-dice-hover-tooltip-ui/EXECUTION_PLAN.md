# F014 执行计划

状态：程序已静态接入；待 Unity 运行验收
功能：骰子悬浮窗 UI
最后更新：2026-07-23

## 就绪度摘要

| 角色 | 就绪度 | 缺口 / 风险 | 建议补丁 | 阻塞问题 |
|---|---|---|---|---|
| 策划 | 就绪 | 无玩法改动 | 维持信息职责边界 | 无 |
| 美术 | 就绪 | 正式无字底件已生成，尚未在 Unity 实机检查 | 程序接入后检查导入与缩放 | 无 |
| 界面体验 | 就绪 | 尚未完成双分辨率实际文字测量 | 用真实文案回归七档内容高度 | 无 |
| 程序 | 已静态实现并独立编译 | Unity Play Mode 尚未完成人工检查 | 按 DEV_HANDOFF.md 验证 | 无 |

## 推荐执行顺序

1. 使用 `$wabish-packet-review` 审查新旧实现交界、字段覆盖和验收可执行性。
2. 使用 `$wabish-art-production` 按已通过母版生成无字运行时素材，并登记 `ART_ASSETS.md`。
3. 已使用 `$wabish-dev-implementation` 接入场景上下文、横向七档布局、实际六面三档格式、结构化状态和数据驱动关键词解释 / 高亮。
4. 在 Unity 完成 `1280x720` / `1920x1080` 市场与主流程截图和动态行为验收。

## 职能任务

| 步骤 | 角色 | 任务 | 输入 | 输出 | 验收 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 美术 / 用户 | 三状态方向样张 | 当前主游戏风格、极限信息样例 | `DiceTooltipV1` 接触图 | 风格、层级和容量获接受 | 已完成 |
| 2 | 跨职能 | 同步新方向生产包 | 用户接受决定 | 六份生产包文档与主文档 | 不再把旧竖版写成目标规格 | 已完成 |
| 3 | 跨职能 | 执行审查 | 更新后的生产包 | 审查补丁与就绪结论 | 无隐藏字段、资源或验收缺口 | 待执行 |
| 4 | 美术 | 生成无字运行时底件 | ART_BRIEF.md、已通过样张 | `ui_tooltip_arcade_*` PNG 与 `.meta` | 尺寸、透明边缘、风格和 key 正确 | 已完成 |
| 5 | 程序 | 接入新展示模型、实际六面、布局与关键词解释 | DEV_HANDOFF.md、运行时底件、关键词配置 | `DiceKingDemo.cs`、`MainGameLedFont.cs`、关键词 CSV | 三种上下文、七档内容、三种数字模式和解释可见性正确 | 已静态实现并独立编译 |
| 6 | 程序 / 界面体验 | 双分辨率与动态验收 | Unity Play Mode | 截图、人工反馈、ACCEPTANCE 更新 | 全清单通过 | 待执行 |

## 依赖关系

- 程序可先用程序化深色面板验证结构，但最终视觉验收依赖正式运行时底件。
- 美术生产不依赖玩法改动，必须保持无字、无数字、无价格。
- 正式清理旧资源依赖新方向完成双分辨率验收。
- 接触图只提供方向和容量，不能替代字体清晰度验证。

## 风险和处理

| 风险 | 处理 |
|---|---|
| 静态规则与状态再次混成一段 | 建立只读视图模型和结构化状态行 |
| 五位至亿级数字溢出 | 按六面最大绝对值统一切换单行整数、两行整数或科学计数 + 快速读取 |
| 买价、卖价场景混淆 | hover 候选显式保存 `DiceTooltipContext` |
| 复杂骰高度失控 | 固定宽度与七档高度，同类状态和同词合并 |
| 普通分类词造成重复教学 | 由 `show_explanation` 分离正文替换和解释显示；当前只显示 `feed / devour / shell`，隐藏 `market_floor / family / dual_family` |
| 点阵正文模糊 | 原始目标分辨率、无辉光硬核心先验收 |
| 新资源缺失 | 程序化深色回退，旧资源继续保留 |
| 面板遮挡按钮 | 按场景设锚点优先级并执行安全区 clamp |

## 验证计划

- 静态检查：`git diff --check`，触发标签映射覆盖当前可用 `DieType`。
- 美术检查：三张基础面板、七档内容布局、数字格、类型芯框尺寸和透明边缘。
- 市场截图：三个货架、当前骰袋、右侧避边、买卖价格口径。
- 主流程截图：`Ready / ResultDecision / StageClear`，不显示价格或贡献分。
- 极限内容：基础骰、双家族、负数、`9,999 / 10,000 / 99,999 / 100,000 / 128,000,000`、喂养、吞噬、化壳与幸运龟；海盗头领、小鬼 / 恶魔骰子、光牙和双阵营骰不显示被隐藏说明或空说明区。
- 动态检查：延迟、淡入、淡出、相邻切换、拖拽隐藏、阶段抑制。

## 当前结论

用户已接受美术、术语视觉语法与数字表现方向；无字运行时素材已生成并登记，代码、关键词配置与 LED 字库已静态接入，通过 Unity 2019.4 Roslyn 独立编译、覆盖校验和反射边界测试。当前没有硬阻塞，但尚未完成 Unity Play Mode 双分辨率截图和人工交互回归，因此只能标记 `已静态实现`，不能标记 `已验收`。
