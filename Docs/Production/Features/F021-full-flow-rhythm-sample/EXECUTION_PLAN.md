# F021 执行计划

状态：进行中；F021-S1 已通过，F021-S2-R3 待完整回归；LED V4 与市场浅色实体键帽 V2 已接入；F021-S5-UI1 已静态接入并通过独立编译，待 Play Mode 状态矩阵；其它切片按原门禁推进
功能：F021 全流程节奏样片
来源：2026-07-08 当前对话
编译来源：wabish-production-pipeline

## 就绪度摘要

| 角色 | 就绪度 | 下一步 skill | 阻塞问题 |
|---|---|---|---|
| 策划 | 就绪 | wabish-packet-review | 无 |
| 美术 | 就绪 | wabish-art-production | 无 |
| 界面体验 | 就绪 | wabish-packet-review | 无 |
| 程序 | F021-S1 / S2 共用包已就绪 | wabish-dev-implementation | 结果动态、生命池和市场仍需独立批准 |

### F021-S5-UI1 就绪度补充

| 角色 | 就绪度 | 下一步 skill | 阻塞问题 |
|---|---|---|---|
| 策划 | 就绪 | wabish-packet-review | 无 |
| 美术 | 就绪 | wabish-art-production | 无；三组状态样图已通过 |
| 界面体验 | 就绪 | wabish-packet-review | 无；Tip 位置已修订为全局中上 |
| 程序 | 就绪 | wabish-dev-implementation | 无；用户已授权接入 Unity |

## 2026-07-16 执行覆盖

- 用户已在原粗样片计划之后，分别批准 `F021-S1` 开始界面与 `F021-S2` 主游戏共用运行时包直接进入 Unity；该新指令覆盖下方“样片全部冻结后才启动程序”的旧总门禁，仅限这两个明确切片。
- F021-S2 已完成资源生产、加载 key 登记、Run 绘制接入、缺失回退、双家族拼壳和 V1 / R2 双分辨率技术验收；用户随后继续否定 R2 边框贴合度，R3 改为 `128×128` 连续物理槽并保留公共骰壳 UV。
- R3 自动验收首次遇到 Game View 临时 `875×30` 尺寸误判；截图器现采用两帧稳定门槛、`0.5s` 重试和 `10s` 超时，并常驻监听一次性请求。当前最近门禁是重新聚焦 Unity 后完成 R3 八图，再由用户确认槽位贴合度并人工检查拖拽幽灵 / 插入标记。

## 2026-07-17 LED V4 全局文字执行覆盖

- 用户通过 V4 并将其设为无特别说明时的全局文字标准；该指令覆盖此前“正式字体接入暂停”的门禁，但不扩大市场布局或玩法范围。
- 美术资源、字形 map、三档命名样式、全局代码路由、主菜单烘焙旧字覆盖、双分辨率真值板和独立编译已经完成。
- 后续所有 UI 切片默认直接复用 V4，不再为每个页面重新选择系统字体；只有骰面实体压印数字与 `DICE KING` 实体招牌保留例外。
- 本切片剩余动作是 Unity 完成资源导入后的 Play Mode 画面与交互回归，不依赖 Computer Use 作为日常文字迭代手段。

## 2026-07-17 LED 语义亮度 V2 执行覆盖

1. `已完成`：冻结 V4 `16×16` 字形、共享基线和三档整数灯珠几何，不再改字形。
2. `分级已认可`：10 项文字角色 / 状态合同覆盖 `FocusAction / PrimaryInfo / SecondaryInfo / Ambient / Disabled / InkOnLight / Warning / Success`；V1 同级不一致仅保留为对照。
3. `已完成、待验收`：V2 新增 `brightness_grade / target_oklab_l / tolerance / anchor_role`，并用统一暗底比较条、真实主菜单、市场和 Tooltip Rect 输出双分辨率硬核心、焦点辉光、状态条与 `90%` 压力图。
4. `待用户通过`：V2 通过后再改 `MainGameLedFont.cs` 与 `DiceKingDemo.cs`，把任意颜色和隐式字号选择收口为显式角色及同级亮度目标；主信息禁止自动降到 `Micro`。
5. `机器已通过`：正式字形覆盖、对比度、角色覆盖、硬核心、裁切、1:1、`90%` 和同级等亮检查通过；人工同级观感仍待用户。
6. `后置`：运行实现完成后只做一次自动 Unity 双分辨率冒烟与鼠标 / 键盘交互回归；Computer Use 不进入日常调光循环。

## 执行顺序

### 2026-07-17 市场浅色实体键帽 V2 执行覆盖

1. `已完成`：V1 纯色承载层退回，V2 恢复原键帽纹理并通过用户视觉门。
2. `已完成`：批准字形固化为仓库内静态 600 子集，附可复现脚本、冻结清单和 OFL 许可证。
3. `已完成`：五处浅色键帽接入专用物理像素实心字入口；暗色刷新与其它显示区继续 V4 LED。
4. `已完成`：Unity 自动生成 normal / purchase / leave × 720p / 1080p 六图，运行指标全部通过。
5. `边界保持`：没有改变布局、热区、市场规则、经济、数据、随机或存档；没有使用 Computer Use。

1. 审查 F021 生产包，确认粗样片范围和五锚点先行边界。
2. 美术按 `ART_BRIEF.md` 生成五锚点接触图：主菜单、Ready、超额井喷、轻结算面板、市场。
3. 用户评审五锚点，确认接受、修订或暂停。
4. 五锚点接受后，美术补开始转场、投骰停转、结算蓄势、金币效果和离开市场回环。
5. 界面体验按 `UI_UX_SPEC.md` 检查状态节奏、信息层级和井喷胜场。
6. 合成 35 到 50 秒粗样片或等价分镜长图。
7. 用户评审并调优样片。
8. 样片满意后冻结为导演基准。
9. 再拆正式 Unity 实现切片和运行时资源清单。

## 职能任务

| 步骤 | 角色 | 任务 | 输入 | 输出 | 验收 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 策划 | 核对样片范围和非目标 | `FEATURE_BRIEF.md` | 审查备注 | 无机制误解或范围膨胀 | 待执行 |
| 2 | 美术 | 生成五锚点接触图 | `ART_BRIEF.md` | `Assets/ArtSource/Production/F021/` 下五个锚点关键帧 | 画面有美术表现且风格统一 | 待执行 |
| 3 | 跨职能 | 用户评审五锚点 | 五锚点接触图 | 接受 / 修订 / 暂停结论 | 五锚点通过后才进入过渡帧 | 待执行 |
| 4 | 美术 | 补过渡关键帧和金币效果局部帧 | 已接受五锚点 | 转场、停转、蓄势、金币、回环补帧 | 与锚点风格一致 | 锚点通过后执行 |
| 5 | 界面体验 | 检查状态顺序和信息层级 | `UI_UX_SPEC.md` 和关键帧 | 样片节奏反馈 | 越线、超额、金币、市场回环清楚 | 锚点通过后执行 |
| 6 | 美术 / 界面体验 | 合成粗样片或分镜长图 | 关键帧和节奏规格 | `Assets/ArtSource/Production/F021/preview/` | 可用于用户评审 | 补帧后执行 |
| 7 | 跨职能 | 用户评审后修订样片 | 评审反馈 | 修订版样片 | 用户确认满意 | 待执行 |
| 8 | 程序 | 样片冻结后拆运行时实现切片 | 冻结样片 | 后续子生产包或 DEV_HANDOFF 更新 | 可以进入 Unity 实现 | 后置 |

## 依赖关系

- 五锚点接触图依赖本包已确认的风格和状态顺序。
- 过渡关键帧和样片合成依赖五锚点通过。
- 程序实现依赖样片冻结和运行时资源拆分，不在粗样片阶段启动。

## 建议默认值

- 首轮交付使用五锚点接触图。
- 五锚点通过后，再扩展为 8 到 12 张关键帧加简单动效说明。
- 如果工具条件允许，再合成为 35 到 50 秒视频或动图。
- 先生成完整画面关键帧，不急于切 runtime 素材。
- 先用固定示例读数 `目标 35 / 最终 78` 检验井喷感。

## 阻塞问题

无。

## 验证计划

- 检查样片是否覆盖完整闭环。
- 检查五锚点是否先行通过。
- 检查是否有足够美术表现，而非纯灰盒。
- 检查胜场是否呈现“越线后继续爆”的超额井喷。
- 检查金币效果是否发生在最终分定格后。
- 检查轻面板是否简洁。
- 检查市场是否只演示购买和离开。
- 检查样片资源是否留在 `Assets/ArtSource/Production/F021/`。

## 交接备注

推荐下一步使用 `$wabish-art-production` 生产五锚点接触图。由于用户已经确认需要有美术表现，不建议做纯线框版；由于样片仍是粗样片，也不建议首轮直接制作完整视频或最终运行时切片。

## 2026-07-22 F021-S5-UI1 执行顺序

| 步骤 | 角色 | 任务 | 输入 | 输出 | 验收 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 策划 / 界面体验 | 编译按钮状态、固定文案、提示动效和特殊资格合同 | F021 Round 65–69 | 本生产包六文件补充 | 不需要程序猜测状态含义 | 已完成 |
| 2 | 美术 / 界面体验 | 生成按钮五状态接触图 | `ART_BRIEF.md` | `market_button_five_state_contact_v1_20260722.png` | 五状态同构且差异清楚 | 已完成、用户通过 |
| 3 | 美术 / 界面体验 | 生成提示四节点和混合资格场景图 | `UI_UX_SPEC.md` | 两张源区样图 | 动效节奏和血契边界可见 | 已完成、用户通过 |
| 4 | 跨职能 | 用户评审三组样图 | 三张样图 | 接受 / 修订 / 暂停 | 视觉合同关闭 | 已完成；Tip 位置修订为全局中上 |
| 5 | 跨职能 | 使用 `$wabish-packet-review` 做实现前审查 | 更新后的生产包 | 审查补丁 | 无遗漏状态或相互冲突 | 已完成、程序就绪 |
| 6 | 程序 | 统一状态入口、固定动作文案和短时提示控件 | 已通过样图与审查包 | Unity 实现与自动状态矩阵 | 不改市场规则、经济、随机或存档 | 代码与静态验证完成；待 Play Mode |

当前执行切片为 `F021-S5-UI1` 第 6 步的动态尾项：代码实现、独立编译和静态合同验证已完成；待运行编辑器刷新最新脚本后补 720p / 1080p 状态矩阵与真实点击回归。

## 2026-07-17 LED 亮度运行修复执行补充

| 步骤 | 内容 | 状态 |
|---|---|---|
| 1 | 固化 V2 同级等亮角色与等级合同为正式 Resources 单一来源 | 已完成 |
| 2 | 主菜单、局内、市场与 Tooltip 关键文字迁移到语义角色入口 | 已完成 |
| 3 | L2 / L3 禁止退化到 Micro；回排关键安全框 | 已完成 |
| 4 | 自动生成主菜单 / 局内 / 市场原生 720p 与 1080p 截图 | 已完成、通过 |
| 5 | 以反馈现场 `0.606×` 生成确定性非整数压力图和前后对照 | 已完成、通过 |
| 6 | 回归浅色实体键帽实心字、同线与原纹理守恒 | 已完成、通过 |
| 7 | 用户查看本轮运行结果并给出最终视觉结论 | 待确认 |

本补充不授权改动玩法、经济、市场规则、数据、随机或存档；自动 `PASS` 不代替第 7 步。
