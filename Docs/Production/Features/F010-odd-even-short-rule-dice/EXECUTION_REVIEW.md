# F010 执行审查

状态：已归档；后续门禁已解除
生产包：Docs/Production/Features/F010-odd-even-short-rule-dice/
来源设计对话编号：D1
最后更新：2026-06-15

## 审查结论

就绪度：本审查当时结论为美术可执行、程序阻塞；该结论已被后续接触图验收和实现裁决解除。

原因：七颗骰的短规则、第一阶段范围、美术目标路径、避免项和接触图验收标准已经足够支持接触图生产；程序实现当时仍依赖接触图接受和规则边界确认，不能在本阶段启动。2026-06-15 后续已完成第二版接触图验收、最终资源生成和首版程序静态接入。

## 摘要

本包曾通过 F010-02 接触图阶段审查，并使用 `$wabish-art-production` 生成七颗奇偶短规则骰接触图，产物保存在 `Assets/ArtSource/Production/F010/`。

本包当时不通过 F010-05 程序实现审查。后续固定分口径、补全边界、复核时机和转号面组限制已作为实现裁决写入 `FEATURE_BRIEF.md` 和 `DEV_HANDOFF.md`。

## 建议改进

### 推荐生产包补丁

- 在 `ACCEPTANCE.md` 中记录本次 `$wabish-packet-review` 已完成，并标注只批准接触图阶段。
- 接触图生成后，在 `ACCEPTANCE.md` 增加产物路径和视觉检查结果。
- 不把接触图登记为最终运行时资源，不更新 `ART_ASSETS.md` 的最终图标表。

### 需要用户决策的阻塞问题

当前没有阻塞接触图生成的用户决策。

程序实现前仍需确认：

- 固定分 `6 / 8` 是否作为首版数值保留。
- `补全骰` 是否只影响全奇 / 全偶副牌型识别。
- `复核骰` 是否只在自然开盅后触发，出千确认后不触发。
- `转号骰` 是否采用出千重摇只抽相反奇偶面的实现。

### 非阻塞后置细节

- 市场章节、价格和权重可以等程序包再定。
- 最终透明图标命名和裁切可以等接触图接受后再定。
- 多颗补全或复核同时存在的触发顺序不影响当前接触图，但会影响程序实现。

## 职能就绪度

| 角色 | 状态 | 原因 | 下一步 skill |
|---|---|---|---|
| 策划 | 就绪 | 七颗短规则和第一阶段边界已锁定。 | `$wabish-art-production` |
| 美术 | 就绪 | 资源清单、视觉意图、路径和避免项清楚。 | `$wabish-art-production` |
| 界面体验 | 需补充 | 短标签已有建议，但未到 UI 实现阶段。 | `$wabish-packet-review` |
| 程序 | 阻塞 | 接触图未接受，且规则边界未做程序裁决。 | `$wabish-dev-implementation` |

## 执行顺序

1. 使用 `$wabish-art-production` 生成一张七颗骰近正方形九宫格接触图，保存在 `Assets/ArtSource/Production/F010/`。
2. 用户评审接触图：接受、继续修订或暂停。
3. 接触图接受后，再生成最终透明图标。
4. 最终图标或至少运行时加载 key 确认后，才进入 `$wabish-dev-implementation`。

## 建议写回生产包的补丁

| 文件 | 章节 | 建议改动 | 原因 | 状态 |
|---|---|---|---|---|
| ACCEPTANCE.md | 第一阶段验收清单 | 勾选 `$wabish-packet-review` 审查完成 | 记录门禁通过 | 已写回 |
| ACCEPTANCE.md | 职能验证记录 | 增加审查结论和接触图产物路径 | 后续评审可追踪 | 已写回 |
| ART_BRIEF.md | 资源清单 | 生成后把接触图状态改为样张待验收 | 准确反映美术状态 | 已写回 |

## 风险

| 风险 | 影响 | 缓解方式 | 负责人 |
|---|---|---|---|
| 三颗出千骰视觉过近 | 玩家分不清翻号、守号、转号 | 接触图中分别使用翻面印、同色锁扣、分界转盘 | 美术 |
| 补全和复核都像“修正” | 玩家误解一个是牌型补全、一个是重摇 | 补全用合拢环，复核用内嵌回环刻线 | 美术 |
| 接触图被误当最终图标 | 提前进入运行时资源或代码依赖 | 只保存到 `Assets/ArtSource/Production/F010/`，不写 `Assets/Resources/Art/DiceTypes/` | 跨职能 |
| 程序默认值被美术阶段固化 | 未评审规则进入代码 | 当前审查只批准接触图，不批准程序实现 | 策划 / 程序 |

## 已记录的用户决策

- 范围锁定七颗：`异邻骰 / 同邻骰 / 补全骰 / 复核骰 / 翻号骰 / 守号骰 / 转号骰`。
- 第一阶段只准备规则卡和接触图。
- 不改代码、不生成最终运行时资源。
- 接触图通过后再进入程序实现。

## 最终建议

本文件保留为当时的包审查记录。当前最新状态以 `ACCEPTANCE.md`、`DEV_HANDOFF.md` 和 `EXECUTION_PLAN.md` 为准：F010 已完成最终图标、程序接入和 Unity Play Mode 验证。
