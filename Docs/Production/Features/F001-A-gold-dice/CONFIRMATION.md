# F001-A 确认记录

状态：已确认
功能：F001-A 金币骰规则细化
确认日期：2026-06-04

## 用户确认

用户已确认：

- 接受 F001-A 执行审查建议。
- 投资骰允许每个小关开始自动锁定预算。
- 取消国库骰单颗加分上限，其他规则保持不变。
- 本次只更新 F001-A 生产包文档，不改代码、不写 CSV、不生成资源。

## 已确认规则摘要

| 骰子 | 已确认默认值 | 关键边界 |
|---|---|---|
| 国库骰 | `floor(小关开始钱包 / 10)` 单骰加分，无单颗上限。 | 读取小关开始钱包快照，不读取本关账本金币。 |
| 贿赂骰 | 每 1 金补 +4 最终分；每颗每次最多 2 金；只有能补到通关时才花钱。 | 玩家确认结算后才扣可用钱包，不吃手牌倍率，不花本关账本金币。 |
| 投资骰 | `min(投资骰数量 * 2, floor(小关开始钱包 / 3))` 自动锁定预算；每颗最多 2 金；每金让本骰每次结算 +2 单骰分。 | 小关开始锁定并预付扣除，整关稳定，不因出千或本关账本刷新。 |

## 技术默认值

- 新增类型正式上线前，程序默认采用类型 key 存档，并保留旧类型序号读取兼容。
- 新增市场 CSV 行必须等代码支持后再写入。
- 当前继续游戏恢复到小关开始，投资预算可在重新进入小关时重新计算；小关中途保存是后续范围。

## 下一步

- 美术可按 `ART_BRIEF.md` 生产三颗金币骰图标。
- 程序可按 `DEV_HANDOFF.md` 实现金币骰。低风险顺序为国库骰、贿赂骰、投资骰。
- 实现完成后再同步 `DICE_ARCHETYPES.md`、`GAME_FLOW.md` 和 `PROJECT_CONTEXT.md`。
