# 关间市场浅色实体键帽文字 V2 样张检查

日期：2026-07-17
状态：机器检查与用户视觉均通过；Unity 接入及双分辨率运行验证完成

## 退回原因

V1 为覆盖旧点阵字和锁定 `7:1` 对比，在五处键帽内增加了平整 `#D6B88C` 承载层。该层同时覆盖原键帽的磨损、颗粒、凹凸与局部光影，人工观感像后贴标签。机器对比度通过不能覆盖该材质失败。

## V2 修订

- 从 `Assets/Resources/Art/Market/arcade_market_common_base.png` 恢复五处原始无字键帽。
- 不新增纯色矩形、铭牌、磨皮层、辉光、描边、阴影或磨损遮罩。
- 保留 V1 的 `Noto Sans SC 600`、`#180E08`、字号和居中规则；实心字直接印在原材质上。
- 暗色刷新键及其它玻璃显示区仍使用 V4 LED。

## 自动结果

| 检查 | 结果 |
|---|---|
| 新增纯色底板 | `0`，通过 |
| 原始键帽区域恢复 | `10 / 10`，通过 |
| 纹理方差保留 | `10 / 10`，通过 |
| 最暗 5% 背景对比 | `3.69:1–4.82:1`，通过大号粗体 `3:1` |
| 中位背景对比 | `5.00:1–5.93:1`，通过 `4.5:1` |
| 双分辨率实际文案装入 | 通过；无裁切、无换行 |
| 有效字高 | 720p 为 `21 / 24 px`，1080p 为 `33 / 35 px` |
| 最长文案 | `购买 999 金 / 卖出 999 金 / 交互已锁定` 全部通过 |
| 输出后重采样 | 无 |

机器报告：`Assets/ArtSource/Production/ArcadeDiceKing/MainGameFlowV1/MarketPhysicalKeyLabelsV2/market_physical_key_labels_metrics_v2_20260717.json`。

## 人工门禁结果

用户已确认 V2，可以按约定进入 Unity；“原图材质自然连续”和“保留纹理后实心字仍足够清楚”两项均关闭。运行接入与六图证据见 `Docs/QA/20260717_market_physical_key_labels_runtime_v1.md`；整个流程没有使用 Computer Use。
