# F009 结算来源与回环触发可读性 QA

- 日期：2026-07-22
- Unity：2019.4.33f1
- 验证方式：隔离工程 BatchMode 编译 + 带图形设备 Play Mode 自动注入状态与截帧
- 覆盖阶段：`Scoring`
- 覆盖分辨率：原生 `1920×1080`、原生 `1280×720`、`1920×1080 → 1164×654` 双线性 `0.606×` 显示压力
- 自动结果：`PASS`
- 人工自审：`通过，可交用户确认；未发现 P0 / P1`

## 本轮修正

- 保留 V6B-R4 / R5 已确认的原槽聚焦、单枚积分光晕、玻璃直吸收、积分塔连续承击、隐藏五档和程序音效。
- `SettlementContributionRecord` 新增只读触发语义；展示事件不再把 `Label` 清空。
- 普通贡献在真实来源骰原槽显示短标签，例如 `点数入账`、`养猪·触发器`。
- 当前主槽影响已经结算的其它骰时，真实来源骰显示 `响应·…`；二重唱复制得分效果时显示 `再触发·…`。
- 回环期间主游标保持在触发槽，并在该槽下方显示 `主槽·骰子类型`；局部队列清空后才继续下一个主槽。
- 右侧终端同步显示 `结算中 / 局部连锁` 与当前短标签，承担远距离阅读；它不播放第二套得分动画。
- 新结算路径继续隐藏旧逐骰 `+N`、骰旁抬升和底部大横幅，避免同一语义重复三次。

## 运行场景

| 场景 | 注入构筑 | 预期语义 | 结果 |
|---|---|---|---|
| 类型触发 + 触发器 | `养猪 / 触发器 / 基础×4` | 来源骰显示 `养猪·触发器`，终端同步；无精确贡献值 | 通过 |
| 二重唱回环 | `三只小猪 / 二重唱 / 基础×4`，其它骰带饲料值 | 第一槽显示 `再触发·三只小猪`；第二槽保持 `主槽·二重唱`；无骰间连线 | 通过 |
| 跨槽响应 | `养猪 / 盟约 / 小鬼 / 基础×3` | 左侧实际贡献骰显示 `响应·盟约`；第二槽保持 `主槽·盟约` | 通过 |

自动场景把测试关目标临时设为 `999`，只用于避免截帧时提前进入目标越线回声；该值没有写回项目数据、玩法或存档。

## 截图证据

| 场景 | 1920×1080 | 1280×720 | 0.606× 压力 |
|---|---|---|---|
| 触发器归因 | `20260722_f009_settlement_source_trigger_1920x1080.png` | `20260722_f009_settlement_source_trigger_1280x720.png` | `20260722_f009_settlement_source_trigger_0606.png` |
| 二重唱再触发 | `20260722_f009_settlement_source_retrigger_1920x1080.png` | `20260722_f009_settlement_source_retrigger_1280x720.png` | `20260722_f009_settlement_source_retrigger_0606.png` |
| 盟约跨槽响应 | `20260722_f009_settlement_source_response_1920x1080.png` | `20260722_f009_settlement_source_response_1280x720.png` | `20260722_f009_settlement_source_response_0606.png` |

原生 `1280×720` 的 1:1 代表裁切：

- `20260722_f009_settlement_source_trigger_crop_1280x720.png`
- `20260722_f009_settlement_source_retrigger_crop_1280x720.png`

运行状态：`20260722_f009_settlement_source_runtime_status.txt`。

## 字体与布局门禁

- 所有新增字符都已存在于已批准的 V4 LED atlas / map，缺字数 `0`；没有生成新字形、替换 atlas 或引入系统字体旁路。
- 来源与主槽标签使用登记的 L1 `SecondaryAmber / SecondaryWarm` 语义角色；`1280×720` 下根据真实文字框回退到已登记 `Micro` 整数几何，核心灯珠 `1 px`，没有分数像素缩放。
- 最长来源标签 `再触发·三只小猪` 的 Micro 实测逻辑宽度为 `116 px`，等于来源文字安全框 `116 px`；原生截图确认没有裁切。
- `主槽·二重唱` 的 Micro 实测逻辑宽度为 `81 px`，小于主槽文字安全框 `106 px`。
- 原生两档截图中标签不压骰面数字、不越出投掷区、不覆盖积分塔或右侧终端；`0.606×` 压力图中来源语义与主槽语义仍能识别。
- 字形清晰度、身份与基线沿用已批准 V4 字库；本轮只新增调用文案和容器，没有改动字形像素、亮灯数、基线或角色亮度合同。

详细机器指标见 `20260722_f009_settlement_source_metrics.json`。

## 自审结论

- 通过：真实来源骰、主结算槽与触发内容可同时识别。
- 通过：二重唱回环没有被误读为主游标已经退回第一槽。
- 通过：积分光晕与积分塔仍是动作主焦点，短标签只解释因果。
- 通过：没有逐骰大号 `+N`、百分比、五档名、实体骰位移、骰间连线或共享灯路。
- 通过：旧底部大横幅在新结算路径中已隐藏，避免来源标签、终端与横幅三重重复。
- 未覆盖：真实玩家连续完整一手的主观节奏与声音听感仍需用户在当前 Unity 编辑器或后续构建中确认；自动截图不能替代该人工门禁。

