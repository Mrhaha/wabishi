# 2026-07-22 市场按钮与动作 Tip 静态验证

功能切片：`F021-S5-UI1`

结论：`STATIC PASS / PLAY MODE PENDING`

## 已验证实现

| 检查项 | 结果 |
|---|---|
| 卖出、购买、刷新、离开市场使用固定动作文案 | PASS |
| 商品按钮资格继续读取 `CanBuyMarketOffer` | PASS |
| 空货架不创建伪购买按钮 | PASS |
| 骰子与兼容改造道具商品共用固定“购买”动作 | PASS |
| 受阻按钮保留说明命中，但按钮绘制入口不调用交易方法 | PASS |
| 成功结果映射为“已卖出 / 已购买 / 已刷新” | PASS |
| 受阻原因包含交互锁定、缺少目标、金币不足、骰袋已满等真实分支 | PASS |
| Tip 使用单一可替换槽，重复触发替换文案并重播 | PASS |
| Tip 最终中心为 `1280×720` 虚拟画布约 `(640, 200)` | PASS |
| Tip 时序为 `0.16s` 飞入、`0.50s` 停留、`0.25s` 上浮淡出 | PASS |
| Tip 在 Tooltip 之后绘制，不创建输入热区 | PASS |

## 编译结果

- 使用 Unity 2019.4.33f1 自带 Roslyn 对 `Assets/Scripts/*.cs` 做独立静态编译。
- 结果：`0` 个错误，`9` 个既有 `CS0162` 不可达代码警告。
- 输出：`Temp/CodexChecks/F021S5UI1/Assembly-CSharp.dll`。
- `git diff --check` 通过。

## 范围守恒

- 本切片的运行代码只修改 `Assets/Scripts/DiceKingDemo.cs` 中的市场按钮表现、说明命中与统一动作 Tip 路由；其余改动为规格、生产包与 QA 状态同步。
- 未修改价格、市场池、购买资格函数、金币结算、随机、骰子效果、数据表、存档字段或 `SaveVersion`。
- 现有 `rewardBanner`、日志、购买 / 卖出 / 刷新 / 离场方法与保存节点继续保留；短时 Tip 不替代结构化反馈。

## 尚待 Unity Play Mode 验证

运行中的 Unity 编辑器在本次改动前已停留于旧 Play 状态；桌面控制器虽然识别到唯一项目窗口，但连续两次无法激活该窗口。为避免误点、退出未保存状态或覆盖 `20260717` 历史验收图，本轮没有继续发送窗口输入，也没有生成新的运行截图。

待编辑器退出旧 Play 状态并完成脚本刷新后，补验：

1. `1280×720 / 1920×1080` 下可用、悬停、按下、受阻、受阻点击五状态。
2. “已卖出 / 已购买 / 骰袋已满 / 金币不足”在全局中上位置的飞入、停留和淡出。
3. 受阻点击不扣金、不取货、不发布购买事件；血契船长满袋资格继续可用。
4. 连续点击时单槽替换重播，不堆叠、不拦截下一次输入。

批准样图仍保存在：`Assets/ArtSource/Production/ArcadeDiceKing/MainGameFlowV1/MarketButtonActionSystemV1/`。
