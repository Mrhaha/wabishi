# F009 V4R6 固定正方体骰面滚轴运行 QA

- 日期：2026-07-21
- Unity：2019.4.33f1
- 验证范围：主局六骰的高速滚动、减速停靠、结果锁定与自动结算
- 结论：PASS

## 已验证合同

- 六个骰子槽位保持 `128×128`，全程复用现有正方体家族骰壳；不旋转、不缩放、不拉伸。
- 高速段使用固定通用点数序列 `1 → 4 → 2 → 6 → 3 → 5`，不泄露骰子类型、属性或最终点数。
- 只有中心点数层上下滚动；类型芯与家族外壳保持静止。
- 默认高速段 `0.56s`；随后进入 `0.66s` 停靠段，六槽以 `0.06s` 间隔从左到右停止。
- 真实点数只在单槽进入停靠段后出现；落点包含约 `7px` 越界和 `2.5px` 回弹。
- 默认流程只需首次按下空格；停靠结束后自动进入结算。出千开关开启时仍保留原结果决策分支。

## 自动化验证

- Unity BatchMode 编译退出码为 `0`，未出现 C# 编译错误。
- `WabishF009FaceReelCapture` 在 `1920×1080` 与 `1280×720` 下分别捕获高速、停靠波和自动结算状态。
- 状态文件：`Docs/QA/20260721_f009_face_reel_runtime_status.txt`
- 自动结果：`PASS: fixed square shells, face-only reel motion, left-to-right stop wave and automatic scoring captured at both target resolutions.`

## 运行截图

| 状态 | 1920×1080 | 1280×720 |
|---|---|---|
| 高速滚动 | `20260721_f009_face_reel_fast_1920x1080.png` | `20260721_f009_face_reel_fast_1280x720.png` |
| 左到右停靠波 | `20260721_f009_face_reel_stop_wave_1920x1080.png` | `20260721_f009_face_reel_stop_wave_1280x720.png` |
| 自动结算 | `20260721_f009_face_reel_auto_scoring_1920x1080.png` | `20260721_f009_face_reel_auto_scoring_1280x720.png` |

## 范围边界

- 本次没有新增或改写骰子随机、计分公式、骰子属性、市场、存档结构。
- 本次没有新增运行时美术位图。
- F009 摇骰分支已达到实现验证门槛；结算表现 V6B-R1 仍是独立待确认项。
