# F021 主游戏共用运行时资源包 V1 验收

日期：2026-07-16  
Unity：2019.4.33f1  
场景：`Assets/Scenes/SampleScene.unity`

状态：R3 底图边框贴合与截图器稳定等待已实现；新八图请求待 Unity 刷新消费，用户贴合度与拖拽态仍待回归。

## 验收范围

- 共用老街机电子底板与顶部、左侧积分塔、中央六槽、右侧终端、底部唯一 `Space` 分区。
- 中立、猪、恶魔、龟、海盗五张家族壳，以及由现有家族壳左右拼接的双家族骰。
- `Ready` 类型芯 / 类型名回退、`ResultDecision` 当前有效数字、`Scoring` 当前骰高亮与短反馈。
- `1920x1080` 与 `1280x720` 两档 16:9 Game View。
- 三位数 `123` 与四位数 `1234` 的实际骰面承载。

## 结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| Unity 脚本编译 | R3 PARTIAL | R2 `CS0136` 已关闭；R3 分辨率稳定等待主体已由 Unity 于 16:30 重新生成 `Assembly-CSharp-Editor.dll`。随后增加的常驻请求监听通过独立 Roslyn 编译，待编辑器重新获得焦点后完成最终 Unity 刷新。 |
| Resources 加载 | PASS | 共用底板与五张家族壳均由 `Resources.Load` 成功加载；代码保留资源缺失时的旧主游戏 / 中立壳回退。 |
| 家族识别 | PASS | 中立、猪、恶魔、龟、海盗单壳正确；双家族样本 `BlackSailBat` 正确显示恶魔 / 海盗左右拼壳。 |
| 待机与结果职责 | PASS | 待机中心显示已有类型图标，缺图标时显示短类型名；结果中心只显示当前有效数字。 |
| 三 / 四位数字 | PASS | `123 / 1234` 在两档分辨率均完整、居中、无裁切。数值仅由验收脚本临时注入，截图后恢复真实投骰值。 |
| 结算阶段 | PASS | 左到右提交行为不变；当前骰高亮、分数与事件条已统一为暗色 CRT + 琥珀 / 青色表现。 |
| 适配 | R3 PENDING | R2 八张 PNG 的实际像素尺寸曾通过；R3 修改物理槽后必须重新生成，不复用旧图结论。 |
| 底图槽位与骰壳适配 | R3 CODE PASS / CAPTURE + HUMAN PENDING | 当前候选为六个 `128×128` 物理槽，中心 `X=299+128n / Y=368`；双分辨率画面、边框贴合与拖拽态仍待确认。 |
| 静态检查 | PASS | `git diff --check` 通过。 |

用户人工验收先后补充了底图锚点缺陷 `F021-20260716-FB-001`、槽位尺寸缺口 `F021-20260716-FB-002`、R2 边框仍未贴合 `F021-20260716-FB-004`；自动回归侧关闭了 R2 编译阻断 `F021-20260716-FB-003`，并正在回归 R3 临时 `875×30` 尺寸误判 `F021-20260716-FB-005`。生命池逻辑仍未实现，因此顶部使用中性占位 `生命 --`；通关暖光、失败锈红、重试扫描和生命归零断电属于后续结果分支资源，不在本包范围。

## 截图

- `20260716_main_game_runtime_ready_1920x1080.png`
- `20260716_main_game_runtime_ready_1280x720.png`
- `20260716_main_game_runtime_result_1920x1080.png`
- `20260716_main_game_runtime_result_1280x720.png`
- `20260716_main_game_runtime_scoring_1920x1080.png`
- `20260716_main_game_runtime_scoring_1280x720.png`
- `20260716_main_game_runtime_digits_1920x1080.png`
- `20260716_main_game_runtime_digits_1280x720.png`

自动化入口：`Assets/Editor/WabishMainGameRuntimeCapture.cs`。脚本常驻监听一次性 `Temp/WabishMainGameRuntimeCapture.request`，在当前编辑器进入 Play Mode、设置固定 Game View 尺寸、执行状态切换、截图和像素尺寸复核；完成后退出 Play Mode但不关闭 Unity。首次加载常驻监听补丁仍需正常的 Unity 脚本刷新。

## R1 / R2 回归结果

- 代码位置：`Assets/Scripts/DiceKingDemo.cs`，`DrawArcadeRun` 的 `DrawTableDice` 区域由 `(188,278)` 改为 `(204,301)`，街机单行间距由 `18` 改为 `16`。
- 自动验证：已重新生成上述八张截图，真实像素尺寸正确；`Ready / ResultDecision / 123·1234` 的六个正式槽位共用同一组中心。
- 人工验证：检查拖拽幽灵、插入标记和结算高亮与骰子共同落在凹槽内，且不碰到顺序提示或底部 `SPACE` 区。
- 当前限制：静态截图不能验证拖拽手感与完整动态边界，保留人工门禁。

## R2 槽位尺寸与骰壳覆盖率

- 有效槽：`112×112`；中心 `X=299+128n / Y=369`；中心距 `128`；净距 `16`；最大高亮 `128×128`。
- 原资源：五张家族壳均为 `512×512`，Alpha 有效区宽 `431–447`、高 `456–458`；旧整图缩放会把可见壳压到约 `94–98 × 100`。
- R2 绘制：公共 UV 裁切 `(32,27,448,458)` 后映射到 `112×112`，预计五类可见壳为宽 `107.8–111.8`、高 `111.5–112`。
- 中心内容：类型芯 / 短类型名 / 数字框换算为裁切后局部比例 `18.6%, 15.3%, 62.9%, 59.2%`。
- 双家族：左右半壳在同一个裁切 UV 中线处分割，不能一侧裁切、一侧使用整图。
- 自动验收器：查找 Game View 预设时新增 `FixedResolution` 类型校验；旧失败记录中的 `Expected 1280x720, current 1532x793` 来自误选同尺寸比例项，不是游戏画布本身改变。
- 编译回归：自动验收器初版因两个局部变量同名出现 `CS0136`；重命名后 `Assembly-CSharp-Editor` 编译通过，自动任务成功进入和退出 Play Mode。
- 当前状态：八张截图与固定像素尺寸检查已通过；五类壳、双家族、类型芯和 `123 / 1234` 已完成自动画面检查。仅用户尺寸观感确认与拖拽人工检查待执行。

## R3 底图边框贴合与截图器稳定等待

- 当前物理槽：`128×128`；Rect 为 `(235+128n,304,128,128)`；中心 `X=299+128n / Y=368`；中心距 `128`；逻辑净距 `0`。
- 骰壳：继续裁切公共 UV `(32,27,448,458)` 后映射到物理槽，预计可见壳宽 `123.1–127.7`、高 `127.4–128`。
- 交互边界：结算高亮内缩 `1px`，Ready 选中框内缩 `2px`；拖拽幽灵和插入标记继续复用同一 `slotRects`。
- 原失败：`1920×1080` 首图成功后，切换 `1280×720` 的首个检查帧读到 `875×30`，旧脚本立即抛错并退出。
- 修复：目标尺寸连续稳定两帧才截图；失配时每 `0.5s` 重新应用固定分辨率，`10s` 后才超时；警告和超时同时记录目标与当前尺寸。
- 触发：常驻监听一次性请求文件，后续请求不再依赖额外源码变更触发域重载。
- 已完成：`git diff --check`；截图器独立 Roslyn 编译；稳定等待主体的当前 Unity Editor 编译。
- 待完成：当前一次性请求仍存在，说明 Unity 失焦后尚未刷新并加载常驻监听补丁；重新聚焦 Unity 后应自动消费并重跑八图。状态文件更新为 `PASS` 前，本节保持待回归。
