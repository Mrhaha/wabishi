# F009 V7-R1 / V7-R2 五档正常速动态样片自检

状态：V7-R1 已获用户通过并冻结为最高暴击母版；V7-R2 其余四档完整构建与技术自检通过，等待用户五档回归。

## 方向检查

- [x] V7 / V7-R1 阶段只制作最高档；用户通过后，V7-R2 才继续派生其它四档。
- [x] 宏观结构已由 R6 的“持续加亮 + 短欠压”改成“锁死—吸光—真空—击穿—余震”。
- [x] 真空态主动熄灭非核心区域；击穿态改变整机亮度、轮廓、空间尺度和声场，不只给积分塔加特效。
- [x] 爆发前不显示结果文字；结果在余震后进入稳定态。
- [x] 六颗骰子没有独立飞散，关键骰面和左右计分信息在峰值后恢复可读。
- [x] 未写入运行时资源目录，未修改 `Assets/Scripts/DiceKingDemo.cs`。

## 构建与解码核对

- [x] 主样片为 `1280×720`、`60 fps`、`5.20s`、312 帧，H.264 / AAC。
- [x] 同步画面对照为 `1280×404`、`60 fps`、`5.20s`、312 帧，按设计无音轨。
- [x] 顺序带声对照为 `1280×780`、`60 fps`、`10.00s`、600 帧，保留 R6 与 V7 各自声音。
- [x] 三支 MP4 均通过 FFmpeg 完整解码，没有损坏或提前截断。
- [x] 正式构建后临时帧目录已删除，`_check_*` 调试文件为 0。
- [x] 临时 WAV 为 48 kHz 单声道，主样片编码为 AAC。

## 动态反差核对

- 前段 `0–3.80s` 音频均值约 `-26.2 dB`，峰值约 `-8.3 dB`。
- 真空段 `3.82–4.13s` 音频均值约 `-31.4 dB`，峰值约 `-16.0 dB`。
- 爆发段 `4.14–4.60s` 音频均值约 `-13.7 dB`，峰值约 `-0.3 dB`。
- 从真空到爆发的平均声压差约 `17.7 dB`，且最大峰值只出现在击穿窗口。
- `4.05s` 真空帧接近熄屏，但积分塔核心、六颗骰子轮廓和机柜空间关系仍可辨认。
- `4.17s` 击穿帧由左侧积分塔向全屏释放，主冲击带、射线、边框和整机后坐指向同一爆点。
- `4.72s` 结果帧已回到稳定亮度，`当前 69`、`本关通过` 和 `SPACE 进入市场` 可读。

## 保留风险

- 这版有意把最高档做得略高，击穿峰值可能需要在用户确认爽感上限后回调亮度或射线数量。
- 近黑、瞬时闪光和整机后坐必须提供“减少闪烁 / 减少镜头运动”的运行时降级方案；本轮只验证导演方向，没有批准具体运行时参数。
- 五阶段审查板只用于确认反差结构，最终爽感必须以带声主样片或顺序带声对照判断。

## 决策门

- [x] 用户已明确确认 V7-R1 达到最高暴击爽感基线，母版已冻结。
- [x] 已向下派生远超过、超过、过关和未达到四档源样片。
- [ ] 用户已确认 V7-R2 四档相邻辨识度和第 04 / 05 档强度断层。
- [ ] 用户已批准拆分 Unity 运行时实现、材质 / Shader、音频事件和可访问性参数。

## V7-R1 游戏正常速自检

- [x] 运行时代码确认游戏 `1.0x` 的普通结算事件为 `0.55 秒/击`；V7 样片约 `0.30 秒/击`，问题不是 MP4 编码倍速，而是样片内部时标偏快。
- [x] V7-R1 保持投骰段原速，只把结算开始到释放点按 `2×` 拉长；爆炸、后坐和余震保持原速。
- [x] 主样片为 `1280×720`、`60 fps`、`7.90s`、474 帧，H.264 / AAC。
- [x] 同屏节奏对照为 `1280×404`、`60 fps`、`7.90s`、474 帧，按设计无音轨。
- [x] 顺序带声对照为 `1280×780`、`60 fps`、`13.12s`、786 帧。
- [x] 三支新 MP4 均通过 FFmpeg 完整解码。
- [x] 正常速结算段音频均值约 `-27.2 dB`；近静默核心均值约 `-48.7 dB`；爆发段均值约 `-13.7 dB`、峰值约 `-0.3 dB`。
- [x] 近静默核心到爆发段的平均声压差约 `35.0 dB`；爆发仍集中在约 `6.86s` 的瞬时窗口。
- [x] 临时渲染帧目录已由构建脚本清理；调试关键帧将在交付前移除。

V7-R1 仍是仅源文件节奏模拟。它依据当前运行时 `1.0x` 时长重排，但不是 Unity Play Mode 录屏；用户已经通过导演母版，仍需单独批准运行时实现后才能拆入代码与资源。

## V7-R2 五档派生自检

- [x] 最高暴击主样片直接复用已通过的 `f009_v7_r1_critical_normal_speed_preview_20260722.mp4`，没有重新编码或替换母版。
- [x] 01–04 四条主样片均为 `1280×720`、`60 fps`、`7.90s`、474 帧，H.264 High / AAC 48 kHz 单声道。
- [x] 五档同步视觉对比为 `1920×800`、`60 fps`、`7.90s`，按设计无音轨。
- [x] 五档顺序带声对比为 `1280×780`、`60 fps`、`39.57s`，保留五档各自音轨。
- [x] 四条新增主样片、同步对比、顺序带声对比均通过 FFmpeg 全片解码。
- [x] 01–04 档的继电追灯与 CRT 声音包络已按 `1.0x` 映射逐事件校正，不存在画面拉长而内部音序仍提前结束；校正后重新封装四条主样片并重建顺序带声对比。
- [x] 峰值板可直接读出响应范围的连续升级；稳定结果板保留失败 / 通关二值文案，没有把内部档位名写进游戏画面。
- [x] 临时 474 帧目录均已清理；构建脚本只删除目录内明确命名的临时 `frame_*.png`。

### 相邻档位边界

| 相邻档 | 自检结论 | 主要证据 |
|---|---|---|
| 01 → 02 | 通过技术门 | 01 灯路逆向回收并低频下坠；02 目标扣合并单次正向脉冲 |
| 02 → 03 | 通过技术门 | 03 增加第二次反向继电、终端响应和轻微 CRT 抖动 |
| 03 → 04 | 通过技术门 | 04 扩到整机边框、持续负载、双向扫光与明显回坐 |
| 04 → 05 | 通过技术门 | 04 无近黑 / 静默 / 射线 / 碎片；05 由真空直接切到全屏击穿与强后坐 |

### 音频层级

- 终局 `5.40–7.40s` 最大峰值依次约为 `-4.7 / -3.8 / -2.3 / -1.5 / -0.4 dB`，没有出现较低档峰值反超。
- 第 04 档使用持续机械负载，因此窗口均值高于第 05 档；第 05 档的强度来自近静默到瞬时峰值的动态反差，不以持续响度取胜。
- V7-R1 近静默核心均值约 `-48.7 dB`，爆发段均值约 `-13.7 dB`，约 `35 dB` 的断层仍只存在于最高档。

V7-R2 仍是源样片，不是运行时录屏。技术自检只能证明文件、层级合同和可复现性成立；四档爽感、耐受度和相邻辨识度仍以用户观看带声动态对比后的判断为准。
