# Agent Notes

- At the start of every new task or conversation in this project, read `PROJECT_CONTEXT.md` and `GAME_FLOW.md` before giving design advice or changing code.
- Before dice archetype, scoring, market, or dice-effect work, also read `DICE_ARCHETYPES.md`.
- Keep confirmed game-flow decisions synchronized in `GAME_FLOW.md`.
- Keep stable product direction and implementation cautions synchronized in `PROJECT_CONTEXT.md`.
- Treat `Assets/Scripts/DiceKingDemo.cs` as the current implementation source of truth, then sync any confirmed behavior changes back into the documents above.
